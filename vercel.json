{
  "version": 2,
  "name": "vyra",
  "buildCommand": "npm --prefix web/client run build",
  "outputDirectory": "web/client/dist",
  "installCommand": "npm --prefix web/client install",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}