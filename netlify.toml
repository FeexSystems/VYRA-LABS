[build]
  command = "npm --prefix web/client install && npm --prefix web/client run build"
  publish = "web/client/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
